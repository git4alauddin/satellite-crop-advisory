import PlaceholderPage from "../../components/PlaceholderPage";

export default function TrendsPlaygroundPage() {
  return (
    <PlaceholderPage
      title="Trends Playground"
      summary="Build trend chart/table in isolation with focused API calls."
    />
  );
}
