import PlaceholderPage from "../../components/PlaceholderPage";

export default function AlertsPlaygroundPage() {
  return (
    <PlaceholderPage
      title="Alerts Playground"
      summary="Build alert list and clear behavior with severity context."
    />
  );
}
