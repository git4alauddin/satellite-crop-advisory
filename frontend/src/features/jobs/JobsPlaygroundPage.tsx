import PlaceholderPage from "../../components/PlaceholderPage";

export default function JobsPlaygroundPage() {
  return (
    <PlaceholderPage
      title="Jobs Playground"
      summary="Build and verify NDVI/NDWI/LST run-flow in isolation."
    />
  );
}
